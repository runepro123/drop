services:
  - type: web
    name: drop-server
    env: node
    plan: free
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    disk:
      name: drop-data
      mountPath: /data
      sizeGB: 1